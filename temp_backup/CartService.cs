namespace PizzaDeli.Services;

/// <summary>
/// Giỏ hàng được quản lý hoàn toàn ở phía client qua localStorage.
/// CartService này là stub rỗng, chỉ giữ lại để không phá vỡ DI registration trong Program.cs.
/// </summary>
public class CartService
{
    // Không cần constructor hay phương thức nào – cart logic ở localStorage (JS).
}
